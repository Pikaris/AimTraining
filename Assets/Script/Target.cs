using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class Target : TargetManager
{
    //bool hitted;

    //public bool Hitted
    //{
    //    set
    //    {
    //        hitted = value;

    //        if (hitted)
    //        {
    //            transform.position = new(Random.Range(minX, maxX), Random.Range(minY, maxY), Random.Range(minZ, maxZ));
    //            gameObject.SetActive(true);
    //        }
    //    }
    //}
}
